# motivational-message
A simple Android application that generates a random motivational message upon clicking the button in the middle of the screen.
First, the user must enter their name in order to generate a custom message containing the user's name.
